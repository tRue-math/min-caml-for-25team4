## 第2問
tuple.ml
```OCaml
let a = Array.make 1 (1, (2, 3)) in
let i = 0 in
let b = a.(i) in
let (x, c) = b in
let (y, z) = c in
print_int x; print_int y; print_int z; print_newline ()
```

tuple.optimized
```OCaml
let Ti4.22 =
	INT 1
in
let Ti6.26 =
	INT 2
in
let Ti7.27 =
	INT 3
in
let Tt9.23 =
	TUPLE(Ti4.22, Ti6.26, Ti7.27)
in
let a.11 =
	EXTFUNAPP(create_array [Ti4.22, Tt9.23])
in
let i.12 =
	INT 0
in
let b.13 =
	GET(a.11, i.12)
in
let TUPLE(x.14, c.31, c.32) =
	b.13
in
let Tu3.18 =
	EXTFUNAPP(print_int [x.14])
in
let Tu2.19 =
	EXTFUNAPP(print_int [c.31])
in
let Tu1.20 =
	EXTFUNAPP(print_int [c.32])
in
let Tu10.21 =
	()
in
EXTFUNAPP(print_newline [Tu10.21])
```


## 第3問
test_11.ml
```OCaml
let rec g x y = (x,y) in
let rec f x y z = (x,g y z) in
let a = (2,3) in
let b = f 1 a 4 in
let (x,yzw)=b in
let (yz,w)=yzw in
let (y,z)=yz in
print_int (x+y+z+w)
```

test_11.optimized
```OCaml
let rec g.9 [x.32, x.33, y.11] =
	TUPLE(x.32, x.33, y.11)
in
let rec f.12 [x.13, y.34, y.35, z.15] =
	let Tt8.31 =
		APP(g.9 [y.34, y.35, z.15])
	in
	let TUPLE(Tt8.45, Tt8.46, Tt8.47) =
		Tt8.31
	in
	TUPLE(x.13, Tt8.45, Tt8.46, Tt8.47)
in
let Ti1.29 =
	INT 2
in
let Ti2.30 =
	INT 3
in
let Ti3.27 =
	INT 1
in
let Ti4.28 =
	INT 4
in
let b.17 =
	APP(f.12 [Ti3.27, Ti1.29, Ti2.30, Ti4.28])
in
let TUPLE(x.18, yzw.40, yzw.41, yzw.42) =
	b.17
in
let Ti5.26 =
	(x.18 + yzw.40)
in
let Ti6.25 =
	(Ti5.26 + yzw.41)
in
let Ti7.24 =
	(Ti6.25 + yzw.42)
in
EXTFUNAPP(print_int [Ti7.24])
```