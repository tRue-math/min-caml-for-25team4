## 第1問

fib.parsed
```
LETREC(
 fib [n] = {
  IF(
   LE(
    VAR n,
    INT 1
   )
   VAR n
   ADD(
    APP(
     VAR fib [
      SUB(
       VAR n,
       INT 1
      )
     ]
    ),
    APP(
     VAR fib [
      SUB(
       VAR n,
       INT 2
      )
     ]
    )
   )
  )
 }
 APP(
  VAR print_int [
   APP(
    VAR fib [
     INT 30
    ]
   )
  ]
 )
)
```
k.normalized
```
LETREC(
 fib [n] = {
  IF(
   LE(
    VAR n,
    INT 1
   )
   VAR n
   ADD(
    APP(
     VAR fib [
      SUB(
       VAR n,
       INT 1
      )
     ]
    ),
    APP(
     VAR fib [
      SUB(
       VAR n,
       INT 2
      )
     ]
    )
   )
  )
 }
 APP(
  VAR print_int [
   APP(
    VAR fib [
     INT 30
    ]
   )
  ]
 )
)
```

## 第2問
```
$ dune exec mincaml test/fib-e     
Fatal error: exception Failure("type error line 2, near characters 2")

$ dune exec mincaml test/sum-e
Fatal error: exception Failure("parse error line 3, near characters 11-11")
```

## 問3
```
let rec gcd m n =
if m = 0 then n else
if m <= n then
gcd m (n - m)
else
gcd n (m - n)
in
print_int (gcd 21600 337500)
```

をコンパイルすると下になる
gcd.s
```
.data
.balign    8
.text
gcd.7:                          # 2
    cmpl    $0, %eax                  # 2
    jne    je_else.17                 # 2
    movl    %ebx, %eax                # 2
    ret                            # 2
je_else.17:                     # 2
    cmpl    %ebx, %eax                # 3
    jg    jle_else.18                 # 3
    subl    %eax, %ebx                # 4
    jmp    gcd.7                      # 4
jle_else.18:                    # 3
    subl    %ebx, %eax                # 6
    movl    %ebx, 0(%ebp)             # 6
    movl    %eax, %ebx                # 6
    movl    0(%ebp), %eax             # 6
    jmp    gcd.7                      # 6
.globl    min_caml_start
min_caml_start:
.globl    _min_caml_start
_min_caml_start: # for cygwin
    pushl    %eax
    pushl    %ebx
    pushl    %ecx
    pushl    %edx
    pushl    %esi
    pushl    %edi
    pushl    %ebp
    movl    32(%esp),%ebp
    movl    36(%esp),%eax
    movl    %eax,min_caml_hp
    movl    $21600, %eax              # 8
    movl    $337500, %ebx             # 8
    call    gcd.7                     # 8
    call    min_caml_print_int        # 8
    popl    %ebp
    popl    %edi
    popl    %esi
    popl    %edx
    popl    %ecx
    popl    %ebx
    popl    %eax
    ret
```